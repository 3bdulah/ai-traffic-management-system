name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-asyncio
          pip install -e packages/shared
          pip install -e packages/adaptive-policy
          pip install -e packages/sumo-engine
          pip install -e packages/carla-bridge
          pip install -e packages/cv-pipeline
          pip install -e backend

      - name: Run tests
        run: pytest packages/adaptive-policy/tests backend/smoke_tests -q

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install and build
        run: |
          npm ci
          npm run lint
          npm run build
